Fork of the official convex-rs client to add support for Flutter Rust Bridge bindings.  
All work is done on the `dart-<version>` branches, where `<version>` is the version of the convex-rs we are tracking.
